kind: Service
apiVersion: v1
metadata:
  name: api-backend-blue
spec:
  selector:
    app: api-backend-blue
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30001
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-backend-blue
spec:
  selector:
    matchLabels:
      app: api-backend-blue
  replicas: 1
  template:
    metadata:
      labels:
        app: api-backend-blue
    spec:
      containers:
        - name: api-backend
          image: martynpi/api-backend
          ports:
            - containerPort: 8080
---
kind: Service
apiVersion: v1
metadata:
  name: api-frontend-blue
spec:
  selector:
    app: api-frontend-blue
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30002
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-frontend-blue
spec:
  selector:
    matchLabels:
      app: api-frontend-blue
  replicas: 1
  template:
    metadata:
      labels:
        app: api-frontend-blue
    spec:
      containers:
        - name: api-frontend-blue
          image: martynpi/api-frontend
          ports:
            - containerPort: 8080
---
kind: Service
apiVersion: v1
metadata:
  name: api-tests-blue
spec:
  selector:
    app: api-tests-blue
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 30003
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-tests-blue
spec:
  selector:
    matchLabels:
      app: api-tests-blue
  replicas: 1
  template:
    metadata:
      labels:
        app: api-tests-blue
    spec:
      containers:
        - name: api-tests
          image: martynpi/api-tests
          ports:
            - containerPort: 8080